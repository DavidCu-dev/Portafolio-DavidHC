---
const { class: className, id } = Astro.props;
---
<section id={id} class={`w-full mx-auto lg:w-[900px] ${className}`}>
    <slot />
    <hr class="my-8 border-t border-gray-600 opacity-30" />
</section>